# Multi-source RCD source snapshot

Official artifact: <https://github.com/phamquiluan/RCAEval>

This directory is a compact convenience snapshot of the implementation used by
the Root-Teller evaluation adapter. It intentionally omits upstream datasets,
notebooks, CI files, and generated outputs. Consult the official artifact for
the complete distribution and setup documentation.

Root-Teller adapter:

```text
evaluation/rq1/baseline_adapters/multisource_rcd.py
```

The included files remain subject to their upstream licenses.

